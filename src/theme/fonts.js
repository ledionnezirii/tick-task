// Typography roles for the app.
//
// - display: Fraunces, a warm characterful serif, used for mastheads and titles
//   (carries the personality, used with restraint).
// - mono: Space Mono for dates, counts and small uppercase labels — the "data"
//   face that makes the UI feel logged and intentional rather than default.
// - body text intentionally uses the system font (fast, highly legible) so the
//   serif never has to do long-form reading.
export const fonts = {
  display: 'Fraunces_600SemiBold',
  displayBold: 'Fraunces_700Bold',
  mono: 'SpaceMono_400Regular',
  monoBold: 'SpaceMono_700Bold',
};

export default fonts;
